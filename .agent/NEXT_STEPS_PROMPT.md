# Промпт для следующей фазы: Runtime Debugging (продолжение)

## 📌 Текущая ситуация
Сборка **полностью завершена**. Оба бинарника скомпилированы и слинкованы:
- `build/macos/Generals/generalsv` — 21MB (Generals)
- `build/macos/GeneralsMD/generalszh` — 22MB (Zero Hour)

**Ветка:** `feature/macos-c_make`

**🟢 Игра СТАБИЛЬНО РАБОТАЕТ 35+ секунд! 400+ фреймов, ноль крашей!**
- Все подсистемы инициализируются (40+ initSubsystem: ... - DONE)
- Metal рендеринг работает стабильно (Clear/BeginScene/Present — 360+ циклов)
- **10 runtime-крашей решено**

---

## ✅ Решённые Runtime-проблемы (Фаза 5) — 10 штук

### 1. SIGBUS в MacOSAudioManager::init()
- AVAudioEngine бросал исключение при отсутствии аудиоустройства.
- **Решение:** Обёрнуто в `@try/@catch`, движок продолжает без аудио.

### 2. SIGSEGV в ThingTemplate::parseModuleName
- `TheModuleFactory` был null, `createModuleFactory()` возвращал базовый `ModuleFactory` вместо `W3DModuleFactory`.
- **Решение:** Исправлено в `MacOSMain.mm` — `return new W3DModuleFactory()`.

### 3. SIGBUS в GameClient::init() при вызове setFrameRate() — КЛЮЧЕВОЙ ФИКС
- **Корневая причина:** несовпадение vtable layout между `GameClient` и `MacOSGameClient`.
- `macos_platform` CMake-таргет не имел зависимости `zi_always`, из-за чего `RTS_ZEROHOUR` не был определён при компиляции `MacOSGameClient.mm`.
- Виртуальные функции `notifyTerrainObjectMoved()` и `createSnowManager()` (под `#if RTS_ZEROHOUR`) отсутствовали в vtable — сдвиг на 2 слота.
- При vtable-диспатче `setFrameRate()` процессор прыгал в typeinfo данные → `EXC_BAD_ACCESS (code=2)`.
- **Решение:** Добавлены `zi_always` + `zi_gameengine_include` как PRIVATE зависимости `macos_platform`, include-пути переключены с `Generals/` на `GeneralsMD/`.

### 4. ERROR_INVALID_D3D в DX8Wrapper::Init()
- `LoadLibrary`/`GetProcAddress` стабы в `windows.h` возвращали nullptr, что не давало создать D3D8 интерфейс.
- **Решение:** `LoadLibrary` возвращает маркер `(HMODULE)0x1`, `GetProcAddress("Direct3DCreate8")` маршрутизируется на `_CreateMetalInterface8_Wrapper`.

### 5. ERROR_OUT_OF_MEMORY (0xDEAD0002) — Memory Pool
- Пулы памяти `MetalSurface8` и `MetalTexture8` отсутствовали в таблице `GameMemoryInitPools_GeneralsMD.inl`.
- Система пулов памяти создаёт пулы с `init=0, overflow=0` и ожидает, что размеры будут заданы в таблице PoolSizes.
- **Решение:** Добавлены записи для Metal-классов под `#ifdef __APPLE__`.

### 6. SIGSEGV в GameResultsInterface
- `createNewGameResultsInterface()` возвращал nullptr → `initSubsystem` вызывал `setName()` на null-указателе.
- **Решение:** Создан `StubGameResultsInterface` с no-op методами в `GameSpyStubs.cpp`.

### 7. Пути к шейдерам и ассетам
- `MacOSShaders.metal` не находился при запуске → добавлены дополнительные пути поиска в `MetalDevice8.mm`.
- `.big` файлы не находились → созданы симлинки в директории сборки.

### 8. SIGSEGV в аудио-воспроизведении (scheduleFile)
- `AVAudioEngine` не запущен или формат аудиофайла несовместим → краш в `AVAudioPlayerNode::scheduleFile`.
- **Решение:** Добавлен `@try/@catch` блок и проверка `isRunning` перед воспроизведением.

### 9. SIGABRT — Конфликт Metal driver vs кастомного аллокатора (КРИТИЧЕСКИЙ)
- Глобальный `operator new/delete` перенаправлял ВСЕ аллокации через `DynamicMemoryAllocator`, который добавляет `MemoryPoolSingleBlock` заголовок. Metal/AppKit/libdispatch используют системный malloc, но освобождают через наш `delete` → `BUG_IN_CLIENT_OF_LIBMALLOC`.
- **Решение:** На macOS (`#ifdef __APPLE__`) глобальный `operator new` использует `calloc(1, size)`, `operator delete` — `free()`. Pool-based аллокации (`MEMORY_POOL_GLUE`) по-прежнему через игровой аллокатор. `calloc` гарантирует обнуление (игра полагается на это).

### 10. SIGSEGV в конструкторах W3DBridgeBuffer / Pathfinder
- `m_numBridges` не был инициализирован перед вызовом `clearAllBridges()` в конструкторе → мусорное значение → цикл по неинициализированным объектам.
- Проявилось после перехода с кастомного аллокатора (обнулял память) на системный malloc.
- **Решение:** `m_numBridges=0` перед `clearAllBridges()`. Также `calloc` для обнуления.

---

## 🎉 Текущее состояние

- **Стабильный runtime** — 400+ фреймов, 800+ heartbeats, ноль крашей за 35 секунд
- **Metal рендеринг** — Clear/BeginScene/Present работает стабильно
- **Все подсистемы** — инициализированы без ошибок

---

## ⚠️ ВАЖНО: перед запуском убивать старые процессы!
```bash
killall generalszh 2>/dev/null; sleep 1
./generalszh
```

---

## 🔧 Следующие шаги (в порядке приоритета)

### Шаг 1: Input handling
- StdKeyboard/StdMouse через Cocoa events — проверить обработку ввода мыши и клавиатуры
- Убедиться, что события корректно маршрутизируются в game engine

### Шаг 2: Metal rendering полноценный
MetalDevice8 реализует DX8 интерфейсы через Metal API. При реальной отрисовке (меню, UI) могут вылезти:
- Неинициализированные Metal ресурсы
- Неправильные форматы текстур
- Отсутствующие шейдеры для конкретных эффектов

### Шаг 3: Аудио
MacOSAudioManager использует AVAudioEngine. Сейчас обёрнут в @try/@catch.

### Шаг 4: UI / меню
3D рендеринг и интерфейс — проверка отрисовки элементов меню и интерактивных элементов.

---

## ⚠️ ВАЖНО: Что знать о стабах

### GameSpyStubs.cpp (450+ строк)
Все сетевые функции — заглушки. Это значит:
- Онлайн-мультиплеер **не работает**
- LAN-лобби **не работает**
- Патч-проверка **отключена**
- IME **отключён**
- Движок должен запуститься в **однопользовательском режиме**

### Ключевые компоненты Platform/MacOS/:
| Подсистема | Файлы | Назначение |
|:---|:---|:---|
| **Metal Backend** | `Source/Metal/MetalDevice8.mm` (85KB!) и 5 пар .h/.mm | DX8 → Metal транслятор |
| **Entry Point** | `Source/Main/MacOSMain.mm` | NSApplication, игровой цикл |
| **Game Client** | `Source/Main/MacOSGameClient.mm` | W3D-совместимый клиент |
| **Window** | `Source/Main/MacOSWindowManager.mm` | NSWindow/NSView |
| **Input** | `Source/Main/StdKeyboard.mm`, `StdMouse.mm` | Cocoa events → game input |
| **Audio** | `Source/Audio/MacOSAudioManager.mm` | AVAudioEngine |
| **Display** | `Source/Client/MacOSDisplay.mm` | CoreText рендеринг |
| **D3DX Shims** | `Source/Main/D3DXStubs.mm` | `D3DXCreateTextureFromFileEx` etc. |
| **Stubs** | `Source/Stubs/GameSpyStubs.cpp` | 170+ сетевых/Win32 заглушек |
| **Shaders** | `Source/Main/MacOSShaders.metal` | Metal шейдеры |

---

## ⚠️ ВАЖНО: CMake vtable lesson

`macos_platform` — это STATIC-библиотека, которую линкуют ОБА таргета (Generals и Zero Hour). 
Из-за этого `zi_always` (который определяет `RTS_ZEROHOUR=1`) должен быть **PRIVATE**, чтобы не «утекать» в Generals-таргет.

Include-пути тоже должны быть правильными:
- `GeneralsMD/Code/GameEngine/Include` — для Zero Hour (с `notifyTerrainObjectMoved`, `createSnowManager`)
- `GeneralsMD/Code/GameEngine/Include/Precompiled` — для правильного PreRTS.h

---

## ⚠️ ВАЖНО: Global allocator на macOS

На macOS **НЕЛЬЗЯ** перехватывать глобальный `operator new/delete` через `DynamicMemoryAllocator`:
- Metal/AppKit/libdispatch используют системный malloc и ожидают системный free
- Кастомный аллокатор добавляет заголовок `MemoryPoolSingleBlock` перед данными
- При `delete` из системных фреймворков — попытка разобрать несуществующий заголовок → abort()
- Решение: `calloc/free` для глобальных new/delete, pool-based для `MEMORY_POOL_GLUE`

---

## 📜 Золотые правила (Golden Rules)
1. **Не трогать Core/** (за исключением `#ifdef __APPLE__`): Платформенный код — в `Platform/MacOS/Source/`.
2. **Минимальный `windows.h`**: Добавлять стабы по ошибке сборки, не заранее.
3. **d3d8_stub.h — source of truth**: Все DX8 интерфейсы через stub.
4. **Unified Pipeline**: Весь рендеринг через `MetalDevice8`.
5. **Ориентир на crash log**: Runtime-ошибки отлаживаем итеративно.
6. **PRIVATE deps для zi_always**: Не загрязнять Generals-таргет ZH-определениями.
7. **killall generalszh**: Всегда убивать старые процессы перед запуском!
8. **calloc, не malloc**: На macOS глобальные аллокации через calloc (обнуление!).

## 🏗 Архитектура сборки (Build Flow)
Подробная диаграмма и полный статус: см. `.agent/PORTING_STATUS.md`

## 🚀 Как продолжить (Инструкция для ИИ)
«Runtime debugging, фаза 5. Игра запускается и **стабильно работает 35+ секунд** без крашей. 400+ фреймов через Metal. 10 крашей решено. Следующий фокус: input handling, полноценный рендеринг, аудио. Перед запуском ОБЯЗАТЕЛЬНО: `killall generalszh 2>/dev/null`. Подробности — в `.agent/PORTING_STATUS.md` и `.agent/RENDERING_FLOW.md`.»
